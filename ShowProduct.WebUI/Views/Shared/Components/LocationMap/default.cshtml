@model IEnumerable<Location>
@{
    Layout = null;
}

@foreach (var item in Model)
{
    <iframe src="@item.MapUrl" frameborder="0" style="border:0;" allowfullscreen="" aria-hidden="false" tabindex="0" loading="lazy"></iframe>
}